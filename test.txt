 //Q1-Factorial
  // int num1 = 5;
  // int fac = 1;
  // for (int i = num1; i >= 1; i--) {
  //   fac = fac * i;
  // }
  // print("Factorial of $num1:$fac");
 //Q3 Sum of Series
  // print("Q3:");
  // List<dynamic> n1 = [1, 5, 9, 13, 17, 21, 25, 29, 33, 37];
  // num sum = n1.reduce((value, element) => value + element);
  // print("Sum=$sum");